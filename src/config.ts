// app urls
const BACKEND_URL = `http://localhost:9000`;

export default  {
    links: {
        SIGN_UP: "/sign-up",
    },
    urls: {
        AUTH: `${BACKEND_URL}/auth`,
        SIGN_UP: `${BACKEND_URL}/users`,
    }
}