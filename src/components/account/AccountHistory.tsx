import React, {Component} from 'react';
import {connect} from "react-redux";

export default class AccountHistory extends React.PureComponent <{}> {
    render() {
        return (
            <h1>
               HELLO!
            </h1>
        )
    }
}
// export default connect(null, null)(AccountHistory as any);